name: Destroy

permissions:
  contents: read
  id-token: write

on:

  workflow_dispatch:
    # checkov:skip=CKV_GHA_7: "GitHub Actions workflow_dispatch inputs MUST be empty"
    inputs:
      delay:
        type: string
        required: false
        default: ""

jobs:
  destroy:
    uses: stormreply/slt-workflows/.github/workflows/destroy.yaml@main
    with:
      delay: ${{ inputs.delay }}
    secrets:
      INFRACOST_API_KEY: ${{ secrets.INFRACOST_API_KEY }}
      STORM_LIBRARY_FOR_TERRAFORM: ${{ secrets.STORM_LIBRARY_FOR_TERRAFORM }}
      TERRAFORM_PRIVATE_KEY: ${{ secrets.TERRAFORM_PRIVATE_KEY }}
